{
	"name": "home-share-server",
	"version": "1.0.0",
	"description": "backend service for the home share app. technology is built on express and mongoDB for version 1",
	"main": "server.js",
	"author": "Olalekan Abdulfatah",
	"license": "MIT",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node src/server.js",
		"dev": "nodemon src/server.js",
		"debug": "ndb server.js"
	},
	"dependencies": {
		"dotenv": "^16.0.0",
		"eslint": "^8.10.0",
		"express": "4",
		"mongoose": "5",
		"ndb": "^1.1.5",
		"validator": "^13.7.0"
	},
	"devDependencies": {
		"nodemon": "^2.0.15"
	}
}
